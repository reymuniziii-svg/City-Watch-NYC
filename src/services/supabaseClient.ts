// Supabase client stub — real client will be created when @supabase/supabase-js
// is installed and VITE_SUPABASE_URL + VITE_SUPABASE_ANON_KEY are configured.

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export const supabase: any | null = null;

export function isSupabaseConfigured(): boolean {
  return supabase !== null;
}
